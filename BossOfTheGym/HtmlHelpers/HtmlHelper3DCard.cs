using Microsoft.AspNetCore.Html;
using Microsoft.AspNetCore.Mvc.Rendering;

namespace BossOfTheGym.HtmlHelpers
{
    public static class HtmlHelper3DCard
    {
        public static HtmlString Create3DCard(this IHtmlHelper html)
        {
            string result = "";



            HtmlString htmlResult = new HtmlString(result);
            return htmlResult;
        }
    }
}
